# Environment for building and running "A Gentle Introduction to Predictive Modelling".
# Version ranges are constrained to combinations the book is known to build with.
# They are not tight enough to guarantee identical numbers -- library defaults change --
# so the committed outputs record the environment they came from: python 3.11,
# numpy 2.4, pandas 3.0, scikit-learn 1.8, scipy 1.17.

# book build
jupyter-book>=1.0,<2.0

# core scientific stack
numpy>=2.0,<3.0
pandas>=2.2,<4.0
scipy>=1.11
matplotlib>=3.8
seaborn>=0.13,<0.14

# modelling
scikit-learn>=1.5,<2.0
statsmodels>=0.14

# brain visualization, used for two figures; those cells fall back to a bar plot
# if it is not installed, so the rest of the book works without it
ggseg>=0.1

# confounder testing (chapter 6). Note: mlconfound pins pygam==0.8.0, which calls a
# SciPy sparse-matrix alias removed in newer SciPy; contents/6_validity/validity.ipynb
# restores it in two lines rather than constraining the rest of the environment.
mlconfound>=0.21
